# Monero wallet password (required)
MONERO_WALLET_PASSWORD=your-monero-wallet-password

# Configuration file path (optional, defaults to ./config.yaml)
STEALTH_SWAP_CONFIG=/etc/stealth-swap/config.yaml

# Logging configuration
RUST_LOG=stealth_swap=info,solana_client=warn,monero_client=info

# Webhook URL for failed swaps (optional)
FAIL_WEBHOOK_URL=https://your-webhook.com/alerts

# Optional database encryption key
DATABASE_ENCRYPTION_KEY=your-32-byte-hex-key